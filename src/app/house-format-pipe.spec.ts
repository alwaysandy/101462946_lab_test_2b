import { HouseFormatPipe } from './house-format-pipe';

describe('HouseFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
